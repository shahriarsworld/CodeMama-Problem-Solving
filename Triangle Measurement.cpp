// Problem Statement
// Write a program to calculate the area of a Triangle.

#include <iostream>
using namespace std;

int main() {
    int base, height;
    cin >> base >> height;
    int area = (base * height) / 2;
    cout << area << endl;
    return 0;
}
